# Tests

This directory contains tests for the repository orchestration functionality.

## Test Files

- `test_iflow_orchestration.py` - Tests for iFlow orchestration components

## Running Tests

To run the tests, you'll need to have Python 3 and pytest installed:

```bash
pip install pytest pyyaml
python -m pytest tests/ -v
```